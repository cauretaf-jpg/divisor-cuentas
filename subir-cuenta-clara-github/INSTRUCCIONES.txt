INSTRUCCIONES

Usa primero este archivo:
- subir-cuenta-clara-github.cmd

Si Windows bloquea o falla el .cmd, usa PowerShell manualmente:

cd "C:\Users\caure\Desktop\Paginas\Cuentas"
git status
git add -A
git commit -m "Actualiza Cuenta Clara"
git push

Tambien viene una version PowerShell:
- subir-cuenta-clara-github.ps1

Si PowerShell bloquea el .ps1, no es problema del script; es la politica de ejecucion de Windows.
En ese caso usa el .cmd o los comandos manuales.
